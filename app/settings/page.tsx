import Link from 'next/link';
import { redirect } from 'next/navigation';
import { getAuthSession } from '@/lib/session';
export default async function SettingsPage() { const session = await getAuthSession(); if (!session?.user) redirect('/login'); return <main className="min-h-screen bg-[#f3f7ff] p-6"><div className="mx-auto max-w-4xl"><Link href="/dashboard" className="text-sm font-bold text-blue-600">← Back to dashboard</Link><section className="soft-card mt-5 rounded-2xl p-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-500">Account</p><h1 className="mt-1 text-3xl font-black">Workspace settings</h1><div className="mt-6 space-y-3">{[['Name',session.user.name??'SESH User'],['Email',session.user.email??''],['Plan','SESH Growth'],['Security','Protected with secure sessions']].map(([label,value])=><div key={label} className="flex items-center justify-between rounded-xl border border-slate-200 bg-slate-50 p-4"><span className="font-bold text-slate-700">{label}</span><span className="text-sm text-slate-600">{value}</span></div>)}</div></section></div></main>; }

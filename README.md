# SESH Office

Browser-ready productivity suite with Docs, Sheets, Slides, user accounts, protected workspaces, Prisma, and NextAuth.

Import this repository into Google AI Studio Build for browser preview. The app uses SQLite by default for simple development; use PostgreSQL before production deployment.

Routes: `/`, `/login`, `/dashboard`, `/docs`, `/sheets`, `/slides`, `/settings`.
